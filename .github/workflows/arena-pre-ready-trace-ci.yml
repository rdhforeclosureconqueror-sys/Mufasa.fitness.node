name: Arena Pre-READY Trace Gate

on:
  pull_request:
    paths:
      - "public/arena-push-up.html"
      - "public/arena-pre-ready-trace.js"
      - "public/arena-live-motion-adapter.js"
      - "public/arena-camera.js"
      - "public/arena-diagnostics.js"
      - "public/arena-phone-flow.js"
      - "public/arena-phone-ui.js"
      - "public/arena-pose-calibration.js"
      - "public/push-up-challenge.js"
      - "test/arena-pre-ready-trace.test.js"
      - "test/arena-live-motion-adapter.test.js"
      - "test/arena-diagnostics.test.js"
      - "test/arena-phone-flow.test.js"
      - "test/arena-phone-ui.test.js"
      - "test/arena-pose-calibration.test.js"
      - ".github/workflows/arena-pre-ready-trace-ci.yml"

permissions:
  contents: read

jobs:
  arena-pre-ready-trace:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Syntax check Arena adapters
        run: |
          node --check public/arena-pre-ready-trace.js
          node --check public/arena-live-motion-adapter.js
          node --check public/arena-pose-calibration.js

      - name: Pre-READY trace regression
        run: node --test test/arena-pre-ready-trace.test.js

      - name: Live motion adapter regression
        run: node --test test/arena-live-motion-adapter.test.js

      - name: Push-up pose calibration regression
        run: node --test test/arena-pose-calibration.test.js

      - name: Existing arena diagnostics regression
        run: node --test test/arena-diagnostics.test.js

      - name: Existing phone flow regression
        run: node --test test/arena-phone-flow.test.js

      - name: Existing phone UI regression
        run: node --test test/arena-phone-ui.test.js