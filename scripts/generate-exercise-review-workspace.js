#!/usr/bin/env node
'use strict';

const fs = require('node:fs');
const path = require('node:path');
const metadata = require('../public/exercise-metadata.js');
const {buildReviewExport} = require('./export-exercise-metadata-review.js');

const OUTPUT = path.resolve(__dirname, '../exercise-review');
const NOTICE = 'GENERATED FILE — DO NOT EDIT PRODUCTION METADATA HERE';
const clone = value => JSON.parse(JSON.stringify(value));
const json = value => `${JSON.stringify(value, null, 2)}\n`;
const responseSchema = p => ({responseSchemaVersion:1,reviewerType:'fitness_ai',reviewerName:null,exerciseId:p.exerciseId,schemaVersion:p.schemaVersion,profileVersion:p.profileVersion,metadataFingerprint:p.metadataFingerprint,recommendation:'accept_with_minor_revisions',confidence:'medium',proposals:{instructions:[],cadence:[],safety:[],phrases:[],cameraSetup:[],poseAnalysis:[],thresholds:[],limitations:[]},translationRecommendations:[],questionsForTrainer:[],approval:{status:'not_authorized',mayChangeProductionStatus:false}});
const translationResponse = p => ({translationSchemaVersion:1,exerciseId:p.exerciseId,profileVersion:p.profileVersion,metadataFingerprint:p.metadataFingerprint,sourceLocale:'en-US',targetLocale:'<set targetLocale>',regionalAudience:'<set regionalAudience>',formality:'<set formality>',readingLevel:'<set readingLevel>',translatorType:'fitness_ai',status:'draft_pending_human_approval',translations:{displayName:'',setupCues:[],movementCues:[],safetyCues:[],cadence:{},phrases:{}},warnings:[],termsRequiringTrainerReview:[],meaningChanges:[]});
function snapshot(p){return {exerciseId:p.exerciseId,displayName:p.displayName,schemaVersion:p.schemaVersion,profileVersion:p.profileVersion,metadataAssetVersion:metadata.ASSET_VERSION,metadataFingerprint:p.metadataFingerprint,approvalStatus:p.approvalStatus,instructions:clone(p.instructions),cadence:clone(p.cadence),phrases:clone(p.phrases),poseAnalysis:clone(p.poseAnalysis),limitations:clone(p.limitations)};}
function localizable(p){return {displayName:p.displayName,setupCues:clone(p.instructions.setupCues),movementCues:clone(p.instructions.movementCues),safetyCues:clone(p.instructions.safetyCues),cadence:clone(p.cadence),phrases:clone(p.phrases)};}
function botInput(p){return {handoffSchemaVersion:1,purpose:'secondary_fitness_review',authority:{fitnessBotMayRecommend:true,fitnessBotMayTranslate:true,fitnessBotMayApprove:false,fitnessBotMayPublish:false,humanTrainerRequired:true},exercise:snapshot(p),requestedReview:{instructions:true,cadence:true,safety:true,coachingPhrases:true,cameraSetup:true,poseFeasibility:true,formRules:true,thresholdRationale:true,limitations:true,translationReadiness:true},requiredResponseSchema:responseSchema(p)};}
function identity(p){return `Schema Version: ${p.schemaVersion}\nProfile Version: ${p.profileVersion}\nMetadata Fingerprint: ${p.metadataFingerprint}`;}
function review(p){const rules=p.poseAnalysis.rules;return `${NOTICE}\n\n# Exercise Review: ${p.displayName}\n\n## Identity\n\nExercise ID: ${p.exerciseId}\n${identity(p)}\n\n## Current Status\n\n${p.approvalStatus}. Human review: Pending. Fitness Bot review: Pending. Translation: Pending.\n\n## Quick Summary\n\n**Instruction coaching:** ${[...p.instructions.setupCues,...p.instructions.movementCues].join(' ')} Cadence: ${Object.values(p.cadence).join(' / ')}. These words guide a member and are not automated findings.\n\n**Automated form judgment:** ${p.poseAnalysis.supported?`Supported only for the defined ${p.poseAnalysis.requiredView} camera view and rules below; uncertainty and confidence gates apply.`:'Not supported. Coaching instructions remain available, but the system must not infer form quality.'}\n\n${p.limitations.join(' ')}\n\n## Setup\n\n${p.instructions.setupCues.map(x=>`- ${x}`).join('\n')}\n\n## Movement\n\n${p.instructions.movementCues.map(x=>`- ${x}`).join('\n')}\n\n## Cadence\n\n${Object.entries(p.cadence).map(([k,v])=>`- ${k}: ${v}`).join('\n')}\n\n## Safety\n\n${p.instructions.safetyCues.length?p.instructions.safetyCues.map(x=>`- ${x}`).join('\n'):'- No exercise-specific safety cue is currently defined; trainer decision required.'}\n\n## Coaching Phrases\n\n${Object.entries(p.phrases).map(([pool,items])=>`- ${pool}: ${items.map(x=>`${x.id} — ${x.template}`).join(' | ')||'None'}`).join('\n')}\n\n## Camera Requirements\n\n- Required view: ${p.poseAnalysis.requiredView}\n- Minimum usable frames: ${p.poseAnalysis.minimumUsableFramePercentage}%\n- Minimum overall confidence: ${p.poseAnalysis.minimumOverallConfidence}\n\n## Pose Measurements\n\n${rules.length?rules.map(x=>`- ${x.id}: ${x.measurement}; landmarks ${x.landmarks.join(', ')}`).join('\n'):'- None.'}\n\n## Form Rules\n\n${rules.length?rules.map(x=>`- ${x.id}: ${x.type}; priority ${x.priority}`).join('\n'):'- Automated form judgment is unsupported.'}\n\n## Thresholds and Persistence\n\n${rules.length?rules.map(x=>`- ${x.id}: ${JSON.stringify(x.thresholds)}; landmark confidence ${x.minimumLandmarkConfidence}; affected frames ${x.minimumAffectedFramePercentage}%; consecutive duration ${x.minimumConsecutiveDurationMs} ms.`).join('\n'):'- None.'}\n\n## Technical Limitations\n\n${p.limitations.map(x=>`- ${x}`).join('\n')}\n\n## Decisions Required\n\n- Confirm or revise instruction, cadence, safety, camera, pose rules, and translation source wording.\n- Decide whether automated form judgment remains appropriate.\n\n## Trainer Notes\n\n\n## Trainer Decision\n\nComplete \`trainer-decision.md\`; this snapshot cannot approve or mutate production metadata.\n`;}
function botPrompt(p){return `${NOTICE}\n\n# Fitness Bot Review Request\n\nYou are acting as a secondary fitness-content reviewer. A qualified human trainer will make every final decision.\n\nDo not claim approval. Do not diagnose medical conditions. Do not claim injury prevention. Do not change pose thresholds solely from opinion. Do not rewrite machine metadata. Do not introduce exercises not included in this package. Do not mark trainer_reviewed or approved, publish content, or alter deterministic pose logic.\n\nReview setup accuracy, movement accuracy, cadence clarity, breathing guidance, safety wording, coaching tone, camera feasibility, pose-analysis feasibility, biomechanical limitations, uncertainty language, translation risks, missing information, and conflicts between instructions and measurements.\n\n## Exact Exercise Review Context\n\n${review(p)}\n\n## Required Review Response\n\nReturn exactly these headings: Overall Recommendation (Accept as written / Accept with minor revisions / Substantial revision required / Reject automated form analysis / Insufficient information), Strengths, Concerns, Proposed Instruction Changes, Proposed Safety Changes, Proposed Pose-Analysis Changes, Proposed Phrase Changes, Translation Notes, Questions for the Human Trainer, Structured Decision. Use this JSON contract under Structured Decision:\n\n\`\`\`json\n${JSON.stringify(responseSchema(p),null,2)}\n\`\`\`\n\n## Draft Translation Request (optional)\n\nSet targetLocale, regionalAudience, formality, and readingLevel. Translate meaning rather than word order; preserve coaching tone, uncertainty, safety meaning, and placeholders exactly. Do not add medical or technical claims, change cadence timing or pose logic, or publish. Flag unnatural phrases, regional assumptions, and terms needing trainer review; when uncertain return the source text. Return:\n\n\`\`\`json\n${JSON.stringify(translationResponse(p),null,2)}\n\`\`\`\n`;}
function decision(p){return `${NOTICE}\n\n# Trainer Decision\n\nExercise: ${p.displayName}\nExercise ID: ${p.exerciseId}\n${identity(p)}\n\nThis editable decision is not production metadata. A fingerprint mismatch makes it stale and blocks implementation.\n\n## Human Review\n\n- [ ] Instructions accepted\n- [ ] Instructions require revision\n- [ ] Cadence accepted\n- [ ] Cadence requires revision\n- [ ] Safety language accepted\n- [ ] Safety language requires revision\n- [ ] Camera protocol accepted\n- [ ] Camera protocol requires revision\n- [ ] Pose rule accepted\n- [ ] Pose rule requires revision\n- [ ] Automated form analysis rejected\n- [ ] Translation source wording accepted\n- [ ] Fitness Bot recommendations reviewed\n\n## Approved Changes\n\n## Rejected Fitness Bot Recommendations\n\n## Additional Trainer Changes\n\n## Status Decision\n\n- [ ] Remain draft\n- [ ] Ready for trainer_reviewed\n- [ ] Not ready\n\nTrainer name:\nTrainer role or credential:\nReview date:\nSignature or confirmation:\n`;}
function write(file, content){fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,content,'utf8');}
function generate(output=OUTPUT,sourceProfiles=metadata.profiles){const validation=metadata.validateExerciseRegistry(sourceProfiles);if(!validation.valid)throw new Error(`Metadata validation failed:\n${JSON.stringify(validation.errors,null,2)}`);const profiles=buildReviewExport(sourceProfiles).profiles;if(fs.existsSync(output))fs.rmSync(output,{recursive:true,force:true});fs.mkdirSync(output,{recursive:true});const packages=[];for(const p of profiles){const dir=path.join(output,'exercises',p.exerciseId);const input=botInput(p);packages.push(input);write(path.join(dir,'profile.json'),json(snapshot(p)));write(path.join(dir,'review.md'),review(p));write(path.join(dir,'fitness-bot-review.md'),botPrompt(p));write(path.join(dir,'fitness-bot-input.json'),json(input));write(path.join(dir,'trainer-decision.md'),decision(p));write(path.join(dir,'translation-source.json'),json({translationSchemaVersion:1,sourceLocale:'en-US',exerciseId:p.exerciseId,schemaVersion:p.schemaVersion,profileVersion:p.profileVersion,metadataFingerprint:p.metadataFingerprint,sourceContent:localizable(p),protectedMeaning:{exerciseIdentityMustNotChange:true,safetyMeaningMustNotWeaken:true,uncertaintyMustNotBecomeCertainty:true,correctionMustRemainNonMedical:true,placeholdersMustBePreserved:true}}));write(path.join(dir,'translations','README.md'),`${NOTICE}\n\n# Translation Drafts: ${p.displayName}\n\n${identity(p)}\n\nDrafts must remain \`draft_pending_human_approval\`. Set targetLocale, regionalAudience, formality, and readingLevel. Drafts are never loaded by production runtime.\n`);}
const rows=profiles.map(p=>`| ${p.displayName} | ${p.profileVersion} | ${p.approvalStatus} | Available | ${p.poseAnalysis.supported?'Supported':'Not supported'} | Pending | Pending | Pending | Pending |`).join('\n');write(path.join(output,'SUMMARY.md'),`${NOTICE}\n\n# Exercise Review Summary\n\n| Exercise | Profile Version | Status | Instruction Support | Pose Support | Human Review | Fitness Bot Review | Translation Status | Primary Decision |\n|---|---:|---|---|---|---|---|---|---|\n${rows}\n`);write(path.join(output,'manifest.json'),json({workspaceSchemaVersion:1,metadataAssetVersion:metadata.ASSET_VERSION,sourceLocale:'en-US',exercises:profiles.map(p=>({exerciseId:p.exerciseId,schemaVersion:p.schemaVersion,profileVersion:p.profileVersion,metadataFingerprint:p.metadataFingerprint,path:`exercises/${p.exerciseId}`}))}));write(path.join(output,'fitness-bot-handoff.json'),json({handoffSchemaVersion:1,metadataAssetVersion:metadata.ASSET_VERSION,packages}));write(path.join(output,'README.md'),`${NOTICE}\n\n# Exercise Review Workspace\n\nGenerated from \`public/exercise-metadata.js\` (asset ${metadata.ASSET_VERSION}). Snapshots and editable decisions are detached and never applied automatically. Regenerate with \`npm run generate:exercise-review-workspace\`. A stale version or fingerprint blocks use.\n`);write(path.join(output,'translations-summary.md'),`${NOTICE}\n\n# Translation Summary\n\nSource locale: en-US. All exercises: Pending. All Fitness Bot translations remain \`draft_pending_human_approval\` until structural, placeholder, phrase-ID coverage, missing/extra string, safety-meaning, trainer, fluent-language (when available), exact version/fingerprint, and separate publication approval checks pass. No draft is connected to runtime.\n`);return output;}
if(require.main===module)console.log(`Exercise review workspace: ${generate()}`);
module.exports={OUTPUT,NOTICE,generate,snapshot,botInput,responseSchema};
