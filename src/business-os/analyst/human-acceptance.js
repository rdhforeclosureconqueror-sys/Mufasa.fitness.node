"use strict";
const crypto=require("node:crypto");
const {AnalystHumanAcceptance}=require("./contracts");
function createAnalystHumanAcceptance({organizationId,actor,authorityRef,evidenceRefs,readiness,clock=()=>new Date()}={}){if(!actor?.userId||!["admin","super_admin"].includes(String(actor.role||"").toLowerCase()))throw new Error("authenticated_admin_required");if(!Array.isArray(actor.authenticationEvidenceRefs)||!actor.authenticationEvidenceRefs.length)throw new Error("authentication_evidence_required");if(!authorityRef)throw new Error("authority_reference_required");if(!Array.isArray(evidenceRefs)||!evidenceRefs.length)throw new Error("acceptance_evidence_required");for(const gate of ["ANALYST_PLATINUM_ARCHITECTURE_READY","ANALYST_INTERNAL_INTEGRATION_VERIFIED","ANALYST_LIVE_EVIDENCE_VERIFIED"])if(readiness?.gates?.[gate]!=="PASS")throw new Error("analyst_platinum_prerequisites_not_passed");const acceptedAt=clock().toISOString(),hash=crypto.createHash("sha256").update(JSON.stringify({organizationId,userId:actor.userId,acceptedAt})).digest("hex").slice(0,16);return AnalystHumanAcceptance({id:`analyst-acceptance:${hash}`,organizationId,actorType:"HUMAN",actorId:actor.userId,authorityRef,authenticationEvidenceRefs:actor.authenticationEvidenceRefs,recordedBy:"AUTHENTICATED_ADMIN_BOUNDARY",scope:"ANALYST_PLATINUM",status:"PASS",acceptedAt,evidenceRefs,version:1})}
module.exports={createAnalystHumanAcceptance};
