"use strict";
const STAGES=Object.freeze(["AGENT_IDENTITY","MISSION","GOAL","RUNTIME_STATE","OBSERVATION","CONTEXT","REASONING","PLAN","PLAN_VALIDATION","AUTHORIZATION","EXECUTION","OBSERVATION_RESULT","VERIFICATION","EVIDENCE","MEMORY_HANDOFF","BUDGET_RISK","NEXT_ACTION","COMPLETION","REPORTING"]);
function deriveDiagnostics(outcomes={},at=new Date().toISOString()){let firstFailure=null,blocked=false;const checks=STAGES.map((id,order)=>{const supplied=outcomes[id];let status="NOT_RUN",reason="not_run";if(blocked){status="BLOCKED";reason=`blocked_by:${firstFailure.checkId}`}else if(supplied){status=supplied.status;reason=supplied.reason||id.toLowerCase();if(status==="FAIL"){firstFailure=Object.freeze({checkId:id,reason,derivedAt:at});blocked=true}}return Object.freeze({id,status,reason,order,dependencies:order?[STAGES[order-1]]:[],evidence:supplied?.evidence||[]})});const complete=checks.every(x=>["PASS","NOT_APPLICABLE"].includes(x.status));return Object.freeze({checks:Object.freeze(checks),firstFailure,exitGate:firstFailure?"NO_GO":complete?"PASS":"INCOMPLETE"})}
module.exports={STAGES,deriveDiagnostics};
