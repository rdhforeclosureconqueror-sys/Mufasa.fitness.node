"use strict";
function createWorldClock(startTime){let current=new Date(startTime).getTime(),sequence=0,queue=[];if(!Number.isFinite(current))throw new Error("invalid_simulation_start_time");const order=()=>queue.sort((a,b)=>a.at-b.at||a.sequence-b.sequence);return Object.freeze({now:()=>new Date(current).toISOString(),schedule(event){const at=new Date(event.at).getTime();if(at<current)throw new Error("event_in_past");const x=Object.freeze({...event,at:new Date(at).toISOString(),sequence:++sequence});queue.push(x);order();return x},advance(durationMs){if(!Number.isSafeInteger(durationMs)||durationMs<0)throw new Error("invalid_simulation_duration");current+=durationMs;const due=queue.filter(x=>new Date(x.at).getTime()<=current);queue=queue.slice(due.length);return due},snapshot:()=>({current:new Date(current).toISOString(),sequence,queue:structuredClone(queue)}),restore(s){current=new Date(s.current).getTime();sequence=s.sequence;queue=structuredClone(s.queue);order()}})}
module.exports={createWorldClock};
