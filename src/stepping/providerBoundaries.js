"use strict";
class ProviderAdapterRegistry { constructor() { this.adapters = new Map(); } register(adapter) { if (!adapter?.providerKey) throw new Error("providerKey is required"); this.adapters.set(adapter.providerKey, adapter); } get(providerKey) { return this.adapters.get(providerKey) || null; } }
class ActivityDeduplicationService { findCandidates(activity, existing) { return existing.filter((candidate) => (activity.sourceRecordId && candidate.sourceProvider === activity.sourceProvider && candidate.sourceRecordId === activity.sourceRecordId) || (activity.activityType === candidate.activityType && Math.abs(Date.parse(activity.startedAt) - Date.parse(candidate.startedAt)) < 120000 && Math.abs(activity.distanceMeters - candidate.distanceMeters) < 100)).map((candidate) => ({ activityId: candidate.activityId, exactProviderMatch: candidate.sourceRecordId === activity.sourceRecordId, requiresHumanReview: candidate.sourceRecordId !== activity.sourceRecordId })); } }
module.exports = { ProviderAdapterRegistry, ActivityDeduplicationService };
