const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('panel install must produce the expected DOM owner',()=>{assert.match(src,/PocketPTMotionLabGymCompatibilityPanel\.install\(\)/);assert.match(src,/gymCompatibilityPanel/);assert.match(src,/GYM_COMPATIBILITY_PANEL_RENDER/);});
