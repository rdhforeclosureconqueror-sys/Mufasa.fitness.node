"use strict";
const test=require("node:test");const assert=require("node:assert/strict");
const {createClientTransformationService}=require("../src/services/clientTransformationService");
function store(){let user={userId:"u1"};return{loadUser:()=>structuredClone(user),updateUser:(_id,fn)=>{user=fn(user);return structuredClone(user)}}}
test("return agreement persists all self-return questions",()=>{const service=createClientTransformationService({userStore:store(),clock:()=>Date.parse("2026-09-02T00:00:00Z")});const p=service.saveAgreement("u1",{returnProcess:"Restart next meal.",why:"Feel strong.",whyImportant:"I want my energy back.",whoAffected:"My family.",checkInPreference:"weekly"});assert.equal(p.returnAgreement.returnProcess,"Restart next meal.");assert.equal(p.checkInPreference,"weekly");});
test("baseline requires front and side photos and stores private check-in",()=>{const s=store(),service=createClientTransformationService({userStore:s});assert.throws(()=>service.addCheckIn("u1",{baseline:true,similarClothingAcknowledged:true}),/Baseline requires front and side/);const photo="data:image/jpeg;base64,AA==";const c=service.addCheckIn("u1",{baseline:true,weight:180,measurements:{waist:34},photos:{front:photo,side:photo},similarClothingAcknowledged:true});assert.equal(c.photoPrivacy,"client_and_assigned_trainer_only");assert.equal(c.measurements.waist,34);assert.equal(service.read("u1").checkIns.length,1);});