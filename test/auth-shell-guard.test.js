"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");const html=fs.readFileSync(path.join(__dirname,"../public/workout.html"),"utf8");
test("workout shell delegates protected entry and logout to canonical runtimes",()=>{assert.match(html,/AuthNavigation\.requireUser\(\{/);assert.match(html,/AuthStateRuntime\.logout\(\{ redirectTo:/);assert.doesNotMatch(html,/clearCanonicalAuthState\?\.\("initializeAuth|type="password"|PILOT_BYPASS_USER/)});
test("workout boot waits for canonical protected entry",()=>assert.match(html,/await initializeAuth\(\)/));
