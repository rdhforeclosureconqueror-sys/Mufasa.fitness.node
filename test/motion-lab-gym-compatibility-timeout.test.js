const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('runtime wait is bounded and becomes a truthful first failure',()=>{assert.match(src,/attempts >= 120/);assert.match(src,/did not become ready within 60 seconds/);assert.match(src,/fail\("RUNTIME_READY"/);});
