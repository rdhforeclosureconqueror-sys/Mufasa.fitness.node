const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('diagnostics are copyable and boundary-specific',()=>{assert.match(src,/PocketPTMotionLabGymCompatibilityIntegrationDiagnostics/);assert.match(src,/FIRST FAILURE:/);['RUNTIME_READY','PERSONAL_AVATAR_COMPATIBILITY','GYM_COMPATIBILITY_CONTROLLER','GYM_COMPATIBILITY_PANEL_SCRIPT','GYM_COMPATIBILITY_PANEL_INSTALL','GYM_COMPATIBILITY_PANEL_RENDER'].forEach(stage=>assert.match(src,new RegExp(stage)));});
