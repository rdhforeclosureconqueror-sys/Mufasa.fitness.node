const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('feature waits rather than becoming a second runtime initializer',()=>{assert.match(src,/setInterval/);assert.match(src,/runtimeReady/);assert.doesNotMatch(src,/MotionLabRuntime\.initialize\(/);});
