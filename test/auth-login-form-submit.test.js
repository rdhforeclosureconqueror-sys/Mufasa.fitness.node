"use strict";const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");const read=f=>fs.readFileSync(path.join(__dirname,"..",f),"utf8");
test("canonical login is the only reachable credential form and submitter",()=>{const files=fs.readdirSync(path.join(__dirname,"../public")).filter(x=>/\.(html|js)$/.test(x)),forms=[],submitters=[];for(const f of files){const s=read(`public/${f}`);if(/type="password"/.test(s))forms.push(f);if(/fetch\([^\n]*api\/auth\/\$\{register\?"register":"login"/.test(s)||/AUTH_LOGIN_URL/.test(s))submitters.push(f)}assert.deepEqual(forms,["login.html"]);assert.deepEqual(submitters,["login.js"])});
test("workout uses canonical guard and contains no credential overlay",()=>{const html=read("public/workout.html");assert.match(html,/AuthNavigation\.requireUser/);assert.doesNotMatch(html,/authLoginForm|authOverlay|authPassword|auth-core\.js/);assert.equal(fs.existsSync(path.join(__dirname,"../public/auth-core.js")),false)});
