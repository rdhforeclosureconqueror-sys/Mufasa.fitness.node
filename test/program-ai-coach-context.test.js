"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),fs=require("fs"),os=require("os"),path=require("path");
const {createUserStore}=require("../src/repositories/userStore");const {createCoachContextService}=require("../src/ai/coachContextService");const {createProgramPersistence,createProgramService}=require("../src/program-engine");
test("coach receives authoritative explain-only program context",()=>{const now=Date.parse("2026-08-03T12:00:00Z"),store=createUserStore({userDir:fs.mkdtempSync(path.join(os.tmpdir(),"program-context-"))}),programService=createProgramService({persistence:createProgramPersistence({userStore:store}),clock:()=>now});programService.assign("m",{goal:"strength",trainingDays:3,durationWeeks:4,startDate:"2026-08-03"});const context=createCoachContextService({userStore:store,programService,clock:()=>now}).build("m");assert.equal(context.program.authority,"program_engine");assert.equal(context.program.decisionPolicy,"explain_only");assert.equal(context.program.currentWeek,1);assert.ok(context.program.today);assert.ok(context.program.nextWorkout);});
