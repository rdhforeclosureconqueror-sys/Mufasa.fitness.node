const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');for(const file of ['motion-lab-gym-compatibility-entry.js','motion-lab-gym-compatibility-integration.js'])test(file+' does not autoplay or move the gym avatar',()=>{const src=fs.readFileSync(path.join(__dirname,'../motion-lab',file),'utf8');assert.doesNotMatch(src,/\.play\(|playAnimation|GO_TO_MAT|CONTROL_INTENT|move_and_slide/);});
