"use strict";
const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");
const read=p=>fs.readFileSync(path.resolve(__dirname,"..",p),"utf8");
test("admin first-failure console is linked from client management and auto-runs",()=>{const admin=read("public/admin-members.html"),html=read("public/admin-first-failure.html"),js=read("public/admin-first-failure.js");assert.match(admin,/admin-first-failure\.html/);assert.match(html,/First-Failure Debug/);assert.match(js,/FIRST FAILURE:/);assert.match(js,/run\(\);/);});
test("first-failure diagnostic covers current intake and community dependency chain",()=>{const js=read("public/admin-first-failure.js");for(const marker of ["retention-journey-start.html","private-sessions.html","/api/me/private-coaching/quote","/api/admin/members","/api/me/conversations","/api/billing/plan","/api/me/run-club/profile","/api/me/run-club/board","/api/avatar/upload-contract"])assert.match(js,new RegExp(marker.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));});
test("diagnostic is read-only and blocks downstream checks after dependency failure",()=>{const js=read("public/admin-first-failure.js");assert.doesNotMatch(js,/method:\s*["'](?:POST|PUT|PATCH|DELETE)/);assert.match(js,/Blocked by/);assert.match(js,/failedDep/);assert.match(js,/does not submit quotes|No production data will be written|method:\"GET\"/);});
