const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('authority is verified after each dependency load',()=>{assert.match(src,/PocketPTPersonalAvatarCompatibility\?\.inspect/);assert.match(src,/PocketPTMotionLabGymCompatibility\?\.inspectRuntime/);assert.match(src,/PocketPTMotionLabGymCompatibilityPanel\?\.install/);});
