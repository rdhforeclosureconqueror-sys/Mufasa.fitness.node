const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('integration owns only compatibility dependencies and panel install',()=>{assert.match(src,/personal-avatar-compatibility\.js/);assert.match(src,/motion-lab-gym-compatibility\.js/);assert.match(src,/motion-lab-gym-compatibility-panel\.js/);assert.doesNotMatch(src,/thriller|squat-motion-spec|lunge-motion-spec|push-up-motion-spec/);});
