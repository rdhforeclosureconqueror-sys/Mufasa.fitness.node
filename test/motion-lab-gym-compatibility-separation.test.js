const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');test('entry and orchestrator remain separate modules',()=>{const entry=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-entry.js'),'utf8');const integration=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');assert.ok(entry.length<1500);assert.ok(integration.length>entry.length);assert.match(entry,/motion-lab-gym-compatibility-integration\.js/);});
