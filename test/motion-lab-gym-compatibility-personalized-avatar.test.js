const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');test('integration targets personalized-avatar compatibility authority',()=>{assert.match(src,/PocketPTPersonalAvatarCompatibility/);assert.match(src,/personal-avatar-compatibility\.js/);assert.doesNotMatch(src,/Phase E reference avatar|fallback avatar/);});
