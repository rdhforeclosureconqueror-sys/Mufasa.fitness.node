const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');test('integration uses the current Motion Lab panel route',()=>{const src=fs.readFileSync(path.join(__dirname,'../motion-lab/motion-lab-gym-compatibility-integration.js'),'utf8');assert.match(src,/\/dev\/motion-lab-gym-compatibility-panel\.js/);assert.doesNotMatch(src,/\/dev\/motion-lab-assets\/motion-lab-gym-compatibility-panel\.js/);});
