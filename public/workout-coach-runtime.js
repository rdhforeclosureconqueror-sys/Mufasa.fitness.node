(function initWorkoutCoachRuntime(globalScope) {
  'use strict';
  const global = globalScope || window;
  if (global.__POCKET_PT_WORKOUT_COACH_INITIALIZED) return;
  global.__POCKET_PT_WORKOUT_COACH_INITIALIZED = true;

  const PREFERENCE_KEY = 'POCKET_PT_WORKOUT_COACH_PREFERENCES_V1';
  const DEFAULT_TEMPO = Object.freeze([3, 1, 3]);
  const CADENCE_WINDOW_SECONDS = 12;
  const metadata = global.ExerciseMetadata;
  const registry = metadata?.getDefaultRegistry?.();
  if (!registry) console.warn?.('[WORKOUT_COACH] exercise metadata unavailable; generic coaching only');
  const localFallback = Object.freeze({exerciseId:'generic',displayName:'Exercise',instruction:{setupCues:['Set your starting position.'],movementCues:['Move with control through a comfortable range.']},cadence:{phaseOne:'Move',hold:'Hold',phaseTwo:'Return'},phrases:{encouragement:[{id:'generic_encouragement_1',template:'Keep your rhythm.',supportsName:false}],completion:[{id:'generic_completion_1',template:'Good job.',supportsName:false}],recovery:[{id:'generic_recovery_1',template:'Take a breath.',supportsName:false}]},poseAnalysis:{supported:false,rules:[]},fallback:true});


  let deps = {}, generation = 0, speaking = false, activeExerciseId = null, queue = Promise.resolve();
  const taught = new Set(), timers = new Map(), eventKeys = new Set();
  let preferences = loadPreferences();
  const debug = (()=>{try{return new URLSearchParams(global.location?.search||'').get('debugWorkoutCoach')==='1';}catch(_){return false;}})();
  function log(event, detail={}) { if(debug) console.info('[WORKOUT_COACH]',event,detail); }
  function safePreferredName(profile=global.USER_PROFILE||{}){const raw=String(profile.preferredName||profile.preferred_name||profile.name||'').trim().split(/\s+/)[0]||'';return raw.length<=24&&/^[\p{L}][\p{L}'’-]*$/u.test(raw)?raw:null;}
  function loadPreferences(){try{const value=JSON.parse(global.localStorage?.getItem(PREFERENCE_KEY)||'null');return {workoutVoice:value?.workoutVoice!==false,guidedCoach:value?.guidedCoach!==false,personalizedCoaching:value?.personalizedCoaching==null?Boolean(safePreferredName()):value.personalizedCoaching!==false,postSetFormFeedback:value?.postSetFormFeedback==null?Boolean(global.__POSE_RUNTIME_STATE?.detectorReady&&global.__cameraActive):value.postSetFormFeedback!==false};}catch(_){return {workoutVoice:true,guidedCoach:true,personalizedCoaching:Boolean(safePreferredName()),postSetFormFeedback:false};}}
  function savePreferences(){try{global.localStorage?.setItem(PREFERENCE_KEY,JSON.stringify(preferences));}catch(_){} syncControls();}
  function setPreferences(next={}){preferences={...preferences,...Object.fromEntries(Object.entries(next).map(([k,v])=>[k,Boolean(v)]))};savePreferences();deps.onVoicePreferenceChange?.(preferences.workoutVoice);if(!preferences.workoutVoice)cancel('voice disabled');return getPreferences();}
  function getPreferences(){return {...preferences};}
  function normalizeId(value){return metadata?.normalizeExerciseId?.(value)||String(value||'').trim().toLowerCase().replace(/[\s-]+/g,'_');}
  function getProfile(exercise={}){const found=registry?.resolve(exercise.exerciseId||exercise.id||exercise.name)||localFallback;return registry?.isFallback(found)?{...found,fallback:true,requestedExerciseId:normalizeId(exercise.exerciseId||exercise.id),displayName:exercise.name||found.displayName}:found;}
  function selectPhrase(profile,pool,index=0){const phrases=profile.phrases?.[pool]||[];return phrases.length?phrases[index%phrases.length].template:'';}
  function parseTempo(value){const match=String(value||'').trim().match(/^(\d+)-(\d+)-(\d+)$/);if(!match)return {phases:[...DEFAULT_TEMPO],valid:false,source:'3-1-3'};const phases=match.slice(1).map(Number);if(phases.some(n=>!Number.isFinite(n)||n<0)||phases.reduce((a,b)=>a+b,0)<=0)return {phases:[...DEFAULT_TEMPO],valid:false,source:'3-1-3'};return {phases,valid:true,source:phases.join('-')};}
  function cancel(reason='cancelled'){generation++;for(const id of timers.values())global.clearTimeout(id);timers.clear();eventKeys.clear();queue=Promise.resolve();speaking=false;try{deps.cancelSpeech?.();}catch(_){}log('cue cancelled',{eventType:reason,exerciseId:activeExerciseId});}
  async function speak(text,eventType,token=generation){if(!preferences.workoutVoice||token!==generation)return {ok:false,skipped:true};speaking=true;log('cue start',{eventType,exerciseId:activeExerciseId});try{const result=await (deps.speak?.(text,eventType)??Promise.resolve({ok:false}));if(token===generation)log('cue complete',{eventType,exerciseId:activeExerciseId});return result;}catch(error){log('speech failure category',{eventType,category:/abort/i.test(String(error?.message))?'abort':'unavailable'});return {ok:false};}finally{if(token===generation)speaking=false;}}
  function schedule(key,delayMs,eventType,text){if(eventKeys.has(key)){log('duplicate event suppressed',{eventType,exerciseId:activeExerciseId});return;}eventKeys.add(key);const token=generation;const id=global.setTimeout(()=>{timers.delete(key);if(token===generation)queue=queue.then(()=>speak(text,eventType,token));},Math.max(0,delayMs));timers.set(key,id);log('scheduled event type',{eventType,exerciseId:activeExerciseId});}
  async function prepareSet(exercise={},setIndex=0,totalSets=1){cancel('exercise transition');activeExerciseId=normalizeId(exercise.exerciseId||exercise.id)||'unknown';const token=generation,profile=getProfile(exercise),first=!taught.has(activeExerciseId);const intro=[profile.displayName+'.',setIndex===0?`${totalSets} set${totalSets===1?'':'s'}.`:`Set ${setIndex+1} of ${totalSets}.`];if(setIndex===0)intro.push('Set 1.');if(preferences.guidedCoach&&first){intro.push(...profile.instruction.setupCues,...profile.instruction.movementCues,'Follow my cadence.');taught.add(activeExerciseId);}await speak(intro.join(' '),'instruction',token);if(token!==generation)return false;for(const cue of ['Starting in 3.','2.','1.','Begin.']){await speak(cue,'countdown',token);if(token!==generation)return false;}return true;
  }
  function workStarted(exercise={},setIndex=0,duration=30){activeExerciseId=normalizeId(exercise.exerciseId||exercise.id)||'unknown';const profile=getProfile(exercise),tempo=parseTempo(exercise.tempo),cycle=tempo.phases.reduce((a,b)=>a+b,0);let elapsed=0,cycleIndex=0;while(elapsed+cycle<=Math.min(CADENCE_WINDOW_SECONDS,Math.max(0,duration-7))){const words=[profile.cadence.phaseOne,profile.cadence.hold,profile.cadence.phaseTwo];let offset=elapsed;tempo.phases.forEach((seconds,i)=>{schedule(`cadence:${setIndex}:${cycleIndex}:${i}`,offset*1000,'cadence',words[i]+'.');offset+=seconds;});elapsed+=cycle;cycleIndex++;}
    const encouragementAt=Math.max(elapsed+1,Math.min(duration-7,Math.round(duration*.55)));if(encouragementAt<duration-5)schedule(`encouragement:${setIndex}`,encouragementAt*1000,'encouragement',selectPhrase(profile,'encouragement',setIndex));
    rescheduleFinalCountdown(duration,setIndex);
  }
  function rescheduleFinalCountdown(remaining,setIndex=0){for(const [key,id] of [...timers])if(key.startsWith('final:')){global.clearTimeout(id);timers.delete(key);eventKeys.delete(key);}const start=Math.max(0,(remaining-5)*1000);schedule(`final:${setIndex}:strong`,start,'countdown','Finish strong.');for(let n=5;n>=1;n--)schedule(`final:${setIndex}:${n}`,start+(5-n)*1000,'countdown',String(n)+'.');}
  function personalize(text){const name=preferences.personalizedCoaching?safePreferredName():null;return String(text).replace('{nameSuffix}',name?`, ${name}`:'');}
  function completeSet(exercise={},recoverySeconds=0,formReport=null){cancel('set complete');const profile=getProfile(exercise);let message=formReport?.primaryFeedback?.message||`${selectPhrase(profile,'completion')} Set complete. ${selectPhrase(profile,'recovery')}`;message=personalize(`Nice work{nameSuffix}. ${message}`);if(recoverySeconds)message+=` ${recoverySeconds} seconds recovery.`;speak(message,'completion');return message;}
  function resetSession(){cancel('new session');taught.clear();activeExerciseId=null;}
  function state(){return {speaking,activeExerciseId,taught:[...taught],pending:timers.size,preferences:getPreferences()};}
  function syncControls(){for(const [key,ids] of Object.entries({workoutVoice:['workoutVoiceToggle','detailsWorkoutVoiceToggle'],guidedCoach:['guidedCoachToggle','detailsGuidedCoachToggle'],personalizedCoaching:['personalizedCoachingToggle','detailsPersonalizedCoachingToggle'],postSetFormFeedback:['postSetFormFeedbackToggle','detailsPostSetFormFeedbackToggle']}))for(const id of ids){const el=global.document?.getElementById?.(id);if(el){el.checked=preferences[key];el.setAttribute?.('aria-checked',String(preferences[key]));}}}
  function bindControls(){for(const [id,key] of [['workoutVoiceToggle','workoutVoice'],['detailsWorkoutVoiceToggle','workoutVoice'],['guidedCoachToggle','guidedCoach'],['detailsGuidedCoachToggle','guidedCoach'],['personalizedCoachingToggle','personalizedCoaching'],['detailsPersonalizedCoachingToggle','personalizedCoaching'],['postSetFormFeedbackToggle','postSetFormFeedback'],['detailsPostSetFormFeedbackToggle','postSetFormFeedback']]){const el=global.document?.getElementById?.(id);if(el&&!el.__coachBound){el.addEventListener('change',()=>setPreferences({[key]:el.checked}));el.__coachBound=true;}}syncControls();}
  function configure(next={}){deps={...deps,...next};bindControls();return state();}
  global.WorkoutCoachRuntime={configure,getProfile,parseTempo,getPreferences,setPreferences,safePreferredName,personalize,prepareSet,workStarted,rescheduleFinalCountdown,completeSet,cancel,resetSession,state,constants:{PREFERENCE_KEY,CADENCE_WINDOW_SECONDS,DEFAULT_TEMPO},registry,selectPhrase};
  if(global.document){if(global.document.readyState==='loading')global.document.addEventListener('DOMContentLoaded',bindControls,{once:true});else bindControls();global.addEventListener?.('pagehide',()=>cancel('route change'));}
})(typeof window!=='undefined'?window:globalThis);
