(function(){"use strict";const grid=document.querySelector("#challengeGrid");const safe=(v)=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));async function load(category=""){grid.textContent="Loading challenges…";const body=await fetch(`/api/challenges${category?`?category=${encodeURIComponent(category)}`:""}`).then(r=>r.json());const challenges=body.data?.challenges||[];grid.innerHTML=challenges.map(c=>`<article class="card"><p class="eyebrow">${safe(c.category)}</p><h2>${safe(c.name)}</h2><p class="lead">${safe(c.shortDescription)}</p><div class="meta"><span class="tag">${safe(c.durationWeeks||Math.ceil(c.durationDays/7))} weeks</span><span class="tag">${safe(c.difficultyLabel||c.difficulty.replace("_"," "))}</span><span class="tag">${safe(c.primaryMetric)}</span></div><div class="actions"><a class="button primary" href="/challenges/${encodeURIComponent(c.slug)}">View challenge</a></div></article>`).join("")||"<p>No matching challenges.</p>"}document.querySelector("#filters").addEventListener("click",e=>{if(!e.target.matches("button"))return;document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));e.target.classList.add("active");load(e.target.dataset.category).catch(()=>grid.textContent="Challenges are temporarily unavailable.")});load().catch(()=>grid.textContent="Challenges are temporarily unavailable.")})();
