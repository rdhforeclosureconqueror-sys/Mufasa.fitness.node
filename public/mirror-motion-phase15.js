(function initMirrorMotionPhase15(root,factory){const api=factory(root||globalThis);if(typeof module==='object'&&module.exports)module.exports=api;else{root.PocketPTMirrorMotionPhase15=api;api.install();}})(typeof window!=='undefined'?window:globalThis,function(globalScope){'use strict';
const DEFAULTS=Object.freeze({minAnchorCount:1,maxCompensation:.22,conflictThreshold:.025,releaseFrames:3});
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const state={installed:false,frames:0,conflicts:0,active:false,releaseStreak:0,lastIntent:0,lastAnchorCount:0,lastPattern:'UNKNOWN',lastPhase:'UNKNOWN',lastSourceRootX:0,firstFailingBoundary:'NONE',lastIssue:'NONE',processErrors:0};
function finite(v){return Number.isFinite(Number(v));}
function analyze(packet,options={}){const config={...DEFAULTS,...options};state.frames+=1;try{const context=packet?.exerciseContext||{};const anchors=context.anchors||{};const names=Object.keys(anchors);state.lastAnchorCount=names.length;state.lastPattern=String(context.pattern||'UNKNOWN');state.lastPhase=String(context.phase||'UNKNOWN');const p14=globalScope.PocketPTMirrorMotionPhase14?.diagnostics?.()||globalScope.__mirrorMotionPhase14Diagnostics||{};const applied=finite(options.sourceRootX)?Number(options.sourceRootX):Number(p14.lastAppliedX||0);state.lastSourceRootX=finite(applied)?applied:0;if(names.length<config.minAnchorCount||!finite(applied)){state.active=false;state.lastIntent=0;state.releaseStreak=0;state.firstFailingBoundary='NONE';state.lastIssue=names.length<config.minAnchorCount?'NO_PLANTED_CONTACTS':'PHASE14_ROOT_X_UNAVAILABLE';return {...packet,contactCompensation:{version:2,reviewFirst:true,active:false,rootXIntent:0,anchorCount:names.length,sourceRootX:state.lastSourceRootX,reason:state.lastIssue}};}
const conflict=Math.abs(applied);if(conflict<config.conflictThreshold){state.releaseStreak+=1;if(state.releaseStreak>=config.releaseFrames){state.active=false;state.lastIntent=0;}state.firstFailingBoundary='NONE';state.lastIssue='ROOT_CONTACT_CONFLICT_BELOW_THRESHOLD';return {...packet,contactCompensation:{version:2,reviewFirst:true,active:state.active,rootXIntent:state.lastIntent,anchorCount:names.length,pattern:state.lastPattern,phase:state.lastPhase,sourceRootX:applied,reason:state.lastIssue}};}
state.releaseStreak=0;state.active=true;state.conflicts+=1;state.lastIntent=clamp(-applied,-config.maxCompensation,config.maxCompensation);state.firstFailingBoundary='NONE';state.lastIssue='ROOT_CONTACT_CONFLICT_DETECTED';return {...packet,contactCompensation:{version:2,reviewFirst:true,active:true,rootXIntent:state.lastIntent,anchorCount:names.length,pattern:state.lastPattern,phase:state.lastPhase,sourceRootX:applied,reason:state.lastIssue}};}catch(error){state.processErrors+=1;state.firstFailingBoundary='PHASE15_CONTACT_COMPENSATION_ANALYZE_ERROR';state.lastIssue=String(error?.message||error);return packet;}}
function diagnostics(){return Object.freeze({...state,measuredDepthAuthority:false,avatarRootAuthority:false});}
function diagnosticsText(){const d=diagnostics();return['MIRROR MOTION INTELLIGENCE — PHASE 15',`First failing boundary: ${d.firstFailingBoundary}`,`Frames: ${d.frames}`,`Root/contact conflicts: ${d.conflicts}`,`Compensation active: ${d.active?'YES':'NO'}`,`Last compensation intent: ${d.lastIntent.toFixed(4)}`,`Source root X: ${d.lastSourceRootX.toFixed(4)}`,`Planted contacts: ${d.lastAnchorCount}`,`Exercise: ${d.lastPattern}`,`Exercise phase: ${d.lastPhase}`,`Process errors: ${d.processErrors}`,`Measured depth authority: NO`,`Avatar root authority: NO (review-first)`,`Last issue: ${d.lastIssue}`].join('\n');}
function install(){if(state.installed)return api;state.installed=true;return api;}
function reset(){state.active=false;state.releaseStreak=0;state.lastIntent=0;state.lastAnchorCount=0;state.lastPattern='UNKNOWN';state.lastPhase='UNKNOWN';state.lastSourceRootX=0;state.firstFailingBoundary='NONE';state.lastIssue='RESET';}
const api=Object.freeze({analyze,diagnostics,diagnosticsText,install,reset,DEFAULTS});return api;});
