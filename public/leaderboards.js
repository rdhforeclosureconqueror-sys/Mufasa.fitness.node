(function (win) {
  "use strict";
  const PERIODS=new Set(["xp_weekly","xp_monthly","xp_lifetime"]);
  const token=()=>win.AuthStateRuntime?.getAuthToken?.()||win.APP_AUTH?.token||null;
  const endpoint=path=>{const base=win.RuntimeState?.getBackendOrigin?.()||win.MAAT_BACKEND_ORIGIN||win.__MAAT_RUNTIME_CONFIG__?.backendOrigin||win.location.origin;if(typeof base!=="string"||!/^https?:\/\//i.test(base))throw Object.assign(new Error("Invalid leaderboard endpoint"),{code:"LEADERBOARD_REQUEST_INVALID"});return `${base.replace(/\/+$/,"")}${path}`;};
  const failure=(code,cause)=>Object.assign(new Error(code),{code,cause});
  function classify(error){if(String(error?.code||"").startsWith("LEADERBOARD_"))return error.code;if(error?.name==="TypeError"||error?.name==="SyntaxError"||error?.name==="InvalidCharacterError")return "LEADERBOARD_BROWSER_COMPATIBILITY_ERROR";return "LEADERBOARD_NETWORK_ERROR";}
  async function request(path,options={}){let response;try{response=await win.fetch(endpoint(path),{...options,credentials:"omit",headers:{"content-type":"application/json",authorization:`Bearer ${token()}`,...options.headers}});}catch(cause){throw failure(classify(cause),cause);}let body;try{body=await response.json();}catch(cause){throw failure("LEADERBOARD_RESPONSE_INVALID",cause);}if(!response.ok)throw failure(response.status===404?"LEADERBOARD_NOT_AVAILABLE":"LEADERBOARD_NETWORK_ERROR");if(!body||typeof body!=="object"||!("data" in body))throw failure("LEADERBOARD_RESPONSE_INVALID");return body.data;}
  function validBoard(data,period){if(!data||data.leaderboardId!==period||!Array.isArray(data.entries))throw failure("LEADERBOARD_RESPONSE_INVALID");if(data.nextCursor!=null&&typeof data.nextCursor!=="string")throw failure("LEADERBOARD_CURSOR_INVALID");for(const row of data.entries)if(!Number.isInteger(row.rank)||row.rank<1||typeof row.displayName!=="string"||!Number.isFinite(row.xp))throw failure("LEADERBOARD_RESPONSE_INVALID");return data;}
  async function mount(){const root=document.getElementById("leaderboardExperience");if(!root)return;root.innerHTML='<header><div><p>Universal gamification</p><h2>Leaderboard</h2></div><select aria-label="Leaderboard period"><option value="xp_weekly">Weekly XP</option><option value="xp_monthly">Monthly XP</option><option value="xp_lifetime">Lifetime XP</option></select></header><p class="muted">Net approved XP after authoritative reversals. Push-Up Challenge scores remain separate.</p><div data-board aria-live="polite" data-state="loading">Loading standings…</div><label><input type="checkbox" data-visible> Include me in universal standings</label>';
    const board=root.querySelector("[data-board]"),select=root.querySelector("select"),visible=root.querySelector("[data-visible]");
    const errorState=code=>{board.dataset.state="error";board.setAttribute("role","alert");board.replaceChildren();const message=document.createElement("p");message.textContent="Standings are temporarily unavailable.";const safe=document.createElement("small");safe.textContent=code;const retry=document.createElement("button");retry.type="button";retry.textContent="Retry";retry.dataset.retry="true";retry.onclick=load;board.append(message,safe,retry);win.ClientCapabilityEvidence?.report("universal_leaderboard","CLIENT_RUNTIME_FAILED",code,"standings");};
    async function load(){board.dataset.state="loading";board.removeAttribute("role");board.textContent="Loading standings…";try{const period=select.value;if(!PERIODS.has(period))throw failure("LEADERBOARD_PERIOD_INVALID");const data=validBoard(await request(`/api/me/leaderboards/${encodeURIComponent(period)}?limit=25`),period);board.replaceChildren();if(!data.entries.length){board.dataset.state="empty";board.textContent=data.selfPosition?"No public XP standings yet. Your position is private.":"No eligible XP standings yet.";}else{board.dataset.state="populated";const table=document.createElement("table");table.innerHTML="<thead><tr><th>Rank</th><th>Member</th><th>XP</th></tr></thead>";const tbody=document.createElement("tbody");for(const row of data.entries){const tr=document.createElement("tr");for(const value of [row.rank,row.displayName,row.xp]){const td=document.createElement("td");td.textContent=String(value);tr.append(td);}if(row.isSelf)tr.setAttribute("aria-current","true");tbody.append(tr);}table.append(tbody);board.append(table);}win.ClientCapabilityEvidence?.report("universal_leaderboard","READY","CLIENT_RENDER_SUCCEEDED","standings");}catch(error){errorState(classify(error));}}
    select.addEventListener("change",load);visible.addEventListener("change",async()=>{visible.disabled=true;try{await request("/api/me/leaderboard-preferences",{method:"PUT",body:JSON.stringify({visible:visible.checked,displayNameMode:"alias"})});await load();}catch(error){visible.checked=!visible.checked;errorState("LEADERBOARD_PRIVACY_UPDATE_FAILED");}finally{visible.disabled=false;}});
    try{const data=await request("/api/me/leaderboards");if(typeof data?.preferences?.visible!=="boolean")throw failure("LEADERBOARD_RESPONSE_INVALID");visible.checked=data.preferences.visible;}catch(error){errorState(classify(error));return;}await load();
  }
  win.LeaderboardBrowserContract=Object.freeze({endpoint,classify,validBoard,periods:PERIODS});
  win.addEventListener("DOMContentLoaded",mount);
})(window);
