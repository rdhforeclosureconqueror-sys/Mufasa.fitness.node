"use strict";const token=localStorage.getItem("authToken")||sessionStorage.getItem("authToken")||"",headers={authorization:`Bearer ${token}`,"content-type":"application/json"},root=document.querySelector("#inbox"),esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));async function api(path,o={}){const r=await fetch(path,{...o,headers}),j=await r.json();if(!r.ok)throw Error(j.error?.message||"Request failed");return j.data}async function open(id){const d=await api(`/api/me/conversations/${id}/messages`);root.innerHTML=`<div class="messages">${d.messages.map(m=>`<div class="message ${m.senderUserId===d.conversation.clientUserId?"mine":""}">${esc(m.body)}<br><small>${esc(m.createdAt)}</small></div>`).join("")||"No messages."}</div><form class="composer"><textarea aria-label="Reply" maxlength="4000" required></textarea><button>Reply</button></form>`;root.querySelector("form").onsubmit=async e=>{e.preventDefault();await api(`/api/me/conversations/${id}/messages`,{method:"POST",body:JSON.stringify({body:root.querySelector("textarea").value})});open(id)}}api("/api/me/conversations").then(d=>d.conversations[0]?open(d.conversations[0].id):root.textContent="No coach conversations yet.").catch(e=>document.querySelector("#error").textContent=e.message);
