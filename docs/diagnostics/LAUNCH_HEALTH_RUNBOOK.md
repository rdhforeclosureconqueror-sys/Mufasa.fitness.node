# Launch Health runbook

Confirm both version responses say `2026-07-31-launch-readiness`, no-store headers are present, and the asset token is `20260731-launch-readiness`. Designate a fixture member. Run deterministic diagnostics, then explicitly select Safe External Checks for AI Coach, summarizer, and Stripe. Never infer reachability from configuration. READY notification and leaderboard states require implementation evidence. Do not expose Render service/deploy IDs or secrets. Roll back feature flags and application commit; preserve append-only operational data.
