# Gamification Checks

The console checks capture, workout source, evaluation, read API, operations, writable storage, XP policy, and achievement catalog. The isolated first-workout simulation reads catalogs without writing a member: authoritative `workout.completed`, 100 base XP, `achievement.workout.1_completed`, `badge.workout.1_completed`, 50 reward XP, 150 expected total, one-time celebration, and maximum-one-award duplicate prevention. Runtime replay/worker evidence remains an operator check when no service instance evidence is available.
