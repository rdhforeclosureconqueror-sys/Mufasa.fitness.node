# Test plan

1. Run focused Node contract tests for `motion-lab-gym-compatibility-*`.
2. Confirm `git diff main...HEAD -- motion-lab/motion-lab-bootstrap.js` is empty.
3. Confirm current bootstrap still contains `thriller_motion_catalog`.
4. After the separate page activation checkpoint, initialize Motion Lab and copy the integration FIRST FAILURE report.
5. Load personalized avatar and inspect mapping only after integration reports READY.
