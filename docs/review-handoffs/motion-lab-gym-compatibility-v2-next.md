# Next checkpoint

After this architecture slice is accepted, make the one-line page activation change and verify the integration report reaches `FIRST FAILURE: NONE` / `READY`. Then load the personalized avatar and inspect skeleton -> canonical map -> rest pose -> mapping profile. Only after that profile is accepted should Idle, Walk, and Run preview become the next implementation PR.
