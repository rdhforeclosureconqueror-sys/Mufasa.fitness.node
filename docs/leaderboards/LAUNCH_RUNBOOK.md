# Leaderboard launch runbook

Enable the complete gamification read pipeline and `GAMIFICATION_LEADERBOARDS=true`. Verify lifetime, weekly, and monthly fixtures; ties; opt-out; administrative exclusion; aliases; pagination; self position; mobile table overflow; and that Push-Up rankings remain unchanged. Disable the flag to roll back APIs/UI without changing XP ledger data.
